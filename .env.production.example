# Copy to .env.production and fill in values before deploying.
# PORT and REDIS_URL are set inline in docker-compose.prod.yml — do not duplicate them here.

MATCH_PREFIX=match:
MATCH_TTL_SECONDS=3600
RECONNECT_TIMEOUT_MS=30000
CORS_ORIGIN=http://46.224.231.217:8091
